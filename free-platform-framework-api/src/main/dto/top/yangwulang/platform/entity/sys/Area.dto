
specification AreaListSpecification {
    eq(parentId)
    eq(areaName)
    eq(sort)
    eq(areaType)
}


AreaGetView {
    #allScalars
}

unsafe input AreaSaveInput {
    id
    parentId
    areaName
    sort
    areaType
}

/* AreaListView {
    #allScalars
    children*
} */